# create differernt styles of plots using wrappers
#     plt.style.use('ggplot')
#     plt.fill_between(mean.index, mean - std, mean + std, alpha=0.5)
#     plt.plot(mean, linewidth=1)
#     plt.show()


# def plot_data(df: DataFrame, *args, **kwargs):
#     """Plot the data"""
#     mean = df.mean(axis=1)
